Внедри следующие изменения в проект:

====================================================================

1. Полностью переработать анимацию загрузки

====================================================================

Требования:

Загрузка (loader) сейчас появляется прибитой к верху — это нужно исправить.

Сделать так, чтобы loader всегда был по центру экрана (и по вертикали, и по горизонтали).

Дизайн не придумывай базовый — создай современный, минималистичный loader самостоятельно.

Анимация должна быть плавной, выглядеть премиально.

Поддержка dark/light темы (автоматически).

Где нужно обновить:

Найди компонент, который отвечает за отображение загрузки вакансий, откликов, генерации писем и т.п.

Переработай UI так, чтобы он отображался поверх контента, как модальный overlay, но без затемнения.

Заглушка должна не прыгать и не дергаться, независимо от размера контейнера.

====================================================================

2. Удалить плашку "отклик в очереди"

====================================================================

Сейчас при отправке отклика появляется всплывающая плашка “Отклик в очереди”.
ЭТОТ ЭЛЕМЕНТ НУЖНО УБРАТЬ ПОЛНОСТЬЮ из логики UI.

— никаких попапов, баннеров, тоастов, сообщений.

====================================================================

3. Добавить badge-счетчик над иконкой истории откликов

====================================================================

Что нужно сделать:

Найти иконку “История откликов” (tabs / bottom nav).

Добавить маленький круглый badge, который показывается поверх иконки справа сверху.

Badge должен отображать количество откликов, которые находятся в очереди/в процессе отправки.

Цвет фона badge → #22C55E (green-500)

Цвет текста → white, размер текста → минимальный, но читаемый.

Badge должен быть похож на iOS notification bubble, но в зелёной теме.

Badge должен исчезать полностью, когда нет откликов в очереди.

Реализация:

Создать глобальный store (или использовать уже существующий state), где хранится:

pendingApplicationsCount: number


Каждый раз, когда отклик отправлен в асинхронную очередь, увеличивать count.

Когда отклик успешно отправлён — уменьшать count.

UI должен автоматически обновляться.

====================================================================

4. Где обновить логику

====================================================================

Компонент истории откликов (tab bar / bottom nav).

Логику async apply, где меняется состояние по откликам.

Заменить вызовы показывающие “Отклик в очереди”, удалив UI баннер.

====================================================================

5. Что должен сделать Replit самостоятельно

====================================================================

Сам выбрать лучший способ позиционирования badge (absolute).

Сам выбрать визуальный стиль loader (но минималистичный, современный).

Сам определить структуры CSS / Tailwind классов.

Сам найти место в коде, где отображается текущий loader.

Сам связать состояние async откликов с компонентом badge.

====================================================================

По завершении — вывести список изменённых файлов

И краткое описание, как работает badge и loader