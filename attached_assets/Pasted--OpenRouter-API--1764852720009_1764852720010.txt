Добавь в проект полноценную поддержку генерации сопроводительных писем с помощью OpenRouter API.
Ключ находится в Replit Secrets под именем OPENROUTER_API_KEY, его нельзя хардкодить.

🔹 1. Создай файл src/api/generateLetter.js

Он должен экспортировать функцию:

export async function generateCoverLetter(resume, vacancy) { ... }


Эта функция должна:

принимать текст резюме и объект вакансии

формировать грамотный prompt

отправлять запрос на OpenRouter API

использовать модель "openai/gpt-4.1-mini"

возвращать текст сопроводительного письма

Формат запроса:

POST https://openrouter.ai/api/v1/chat/completions
Authorization: Bearer process.env.OPENROUTER_API_KEY
Content-Type: application/json


Пример тела:

{
  "model": "openai/gpt-4.1-mini",
  "messages": [
    { "role": "system", "content": "Ты помощник, который пишет идеальные сопроводительные письма." },
    { "role": "user", "content": "PROMPT..." }
  ]
}


Пусть функция возвращает контент:

data?.choices?.[0]?.message?.content


Если ошибка — возвращай fallback строку.

🔹 2. Подключи генерацию в свайпы вакансий

В файле, где обрабатывается свайп вправо (App.jsx или VacancySwiper.jsx):

Импортируй функцию:

import { generateCoverLetter } from "./api/generateLetter";


В логике свайпа вправо:

получи сопроводительное письмо:

const letter = await generateCoverLetter(userResume, vacancy);


добавь в историю новое событие:

setHistory(prev => [
  ...prev,
  {
    id: vacancy.id,
    vacancyTitle: vacancy.title,
    company: vacancy.company,
    coverLetter: letter,
    date: new Date().toLocaleString()
  }
]);


Убедись, что пользовательское резюме берётся из редактора профиля (resumeState).

🔹 3. Покажи письмо в «Истории откликов»

В компоненте History:

отобрази:

название вакансии

компанию

дату отклика

сгенерированное сопроводительное письмо (полный текст)

Сделай карточку визуально похожей на другие карточки проекта.

🔹 4. Ничего не переписывай с нуля

Добавляй новые файлы, импортируй функции и корректно обновляй существующие компоненты.